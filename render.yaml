services:
  - type: web
    name: capitalism-io
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: DATA_DIR
        value: /data
    disk:
      name: userdata
      mountPath: /data
      sizeGB: 1
